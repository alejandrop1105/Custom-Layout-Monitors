<UserControl x:Class="CustomLayoutMonitors.Controls.Monitor3DView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="200">
    <Grid Background="Transparent">
        <!-- 3D Viewport for Perspective -->
        <Viewport3D>
            <Viewport3D.Camera>
                <PerspectiveCamera Position="0,0,1.75" LookDirection="0,0,-1" FieldOfView="60"/>
            </Viewport3D.Camera>

            <Viewport3D.Children>
                <!-- Light -->
                <ModelVisual3D>
                    <ModelVisual3D.Content>
                        <AmbientLight Color="White"/>
                    </ModelVisual3D.Content>
                </ModelVisual3D>

                <!-- Monitor Plane -->
                <ModelVisual3D>
                    <ModelVisual3D.Content>
                        <GeometryModel3D>
                            <GeometryModel3D.Geometry>
                                <!-- Plane Mesh (1.6 aspect ratio approx, but texture will stretch) -->
                                <!-- Positions: BL, BR, TR, TL -->
                                <MeshGeometry3D Positions="-1,-0.6,0  1,-0.6,0  1,0.6,0  -1,0.6,0"
                                                TriangleIndices="0,1,2 0,2,3"
                                                TextureCoordinates="0,1 1,1 1,0 0,0"/>
                            </GeometryModel3D.Geometry>
                            <GeometryModel3D.Material>
                                <DiffuseMaterial>
                                    <DiffuseMaterial.Brush>
                                        <ImageBrush ImageSource="{Binding ImageSource, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    Stretch="Fill"/> <!-- Fill to mesh -->
                                    </DiffuseMaterial.Brush>
                                </DiffuseMaterial>
                            </GeometryModel3D.Material>
                            
                            <!-- Rotation Transform -->
                            <GeometryModel3D.Transform>
                                <RotateTransform3D>
                                    <RotateTransform3D.Rotation>
                                        <AxisAngleRotation3D Axis="0,1,0" 
                                                             Angle="{Binding RotationAngle, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                    </RotateTransform3D.Rotation>
                                </RotateTransform3D>
                            </GeometryModel3D.Transform>
                        </GeometryModel3D>
                    </ModelVisual3D.Content>
                </ModelVisual3D>
            </Viewport3D.Children>
        </Viewport3D>
    </Grid>
</UserControl>
